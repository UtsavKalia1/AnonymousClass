@FunctionalInterface
public interface CalInterest {
    double SimpleInterest(double principal,
                          double interest,
                          double rate,
                          double time);
}
